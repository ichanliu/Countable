# 2026-05-14

## APK 建置成功

- 歷經多次記憶體不足失敗（4GB RAM 機器限制），最終成功以 `./gradlew assembleDebug --no-daemon --max-workers=1 -PreactNativeArchitectures=arm64-v8a` 建置成功
- 關鍵調整：
  - `gradle.properties` 設定 `-Xmx768m -XX:MaxMetaspaceSize=256m`
  - 限制 `reactNativeArchitectures=arm64-v8a`
  - 關閉平行編譯 `org.gradle.parallel=false`
  - `kotlin.compiler.execution.strategy=in-process`
- APK 位置：`android/app/build/outputs/apk/debug/app-debug.apk`（24.35MB）
- EAS 雲端專案已建立於 `@ichanliu/countdowns`，但免費隊列等待時間較長
- APK 已透過 deliver_attachments 傳送給用戶
